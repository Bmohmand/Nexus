name: manifest
description: "Manifest — AI search engine for physical assets."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # State management
  flutter_riverpod: ^2.6.1

  # Supabase (auth, database, storage)
  supabase_flutter: ^2.8.0

  # Routing
  go_router: ^14.8.0

  # HTTP client for FastAPI middleware
  dio: ^5.7.0

  # Image picker (camera + gallery)
  image_picker: ^1.1.2

  # Image caching
  cached_network_image: ^3.4.1

  # Environment variables (.env)
  flutter_dotenv: ^5.2.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - .env

import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('ManifestApp smoke test', (WidgetTester tester) async {
    // Basic smoke test placeholder.
    // Full widget test requires Supabase mock setup.
    expect(true, isTrue);
  });
}

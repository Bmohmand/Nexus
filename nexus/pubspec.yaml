name: nexus
description: Physical World API - Transform objects into searchable vector embeddings
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HTTP requests for API integration
  http: ^1.1.0
  
  # Optional: Camera and image features (uncomment when ready to use)
  # camera: ^0.10.5
  # image_picker: ^1.0.4
  # path_provider: ^2.1.1
  
  # Optional: For more advanced features
  # dio: ^5.4.0                    # Alternative HTTP client with better error handling
  # flutter_riverpod: ^2.4.9      # State management
  # cached_network_image: ^3.3.0  # Image caching
  # uuid: ^4.2.1                   # Generate UUIDs

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  
  # Uncomment to add custom fonts
  # fonts:
  #   - family: Geist
  #     fonts:
  #       - asset: fonts/Geist-Regular.ttf
  #       - asset: fonts/Geist-Bold.ttf
  #         weight: 700